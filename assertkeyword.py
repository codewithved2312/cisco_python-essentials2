#The assert keyword in Python is used for debugging and testing — it helps verify that a certain condition in your code is true at runtime.
""" 
x = -5
assert x > 0, "x must be positive"
print("x is positive")

"""

import math

x = float(input("Enter a number: "))
assert x >= 0.0

x = math.sqrt(x)

print(x)
    
